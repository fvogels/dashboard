# Dashboard

Used as new-tab page on your browser.

## Running locally

```bash
$ npx vite run --mode MODE
```

where MODE is the dashboard you want to serve.

## Creating new dashboard

* Create new `.env` file under `environments` directory.
* In `App.tsx`, add case